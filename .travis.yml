sudo: required
services:
  - docker
addons:
   apt:
     sources:
       - git-core
     packages:
       - git

cache:
  directories:
    - $HOME/.m2
    - $HOME/cache/

branches:
    except:    
        - /^([0-9]+)\.([0-9]+)\.([0-9]+)(?:-([0-9A-Za-z-]+(?:\.[0-9A-Za-z-]+)*))?(?:\+[0-9A-Za-z-]+)?$/

install:
  - TRAVIS_WORK_DIR="${PWD}/.."
  - ./scripts/prepare_build_prod_and_run.sh --working-dir=${TRAVIS_WORK_DIR} --store-cache=$HOME/cache/_tmp --use-cache=$HOME/cache/_tmp

script:
  - ./scripts/travis-verify-publish.sh --working-dir=${TRAVIS_WORK_DIR} --browser-test=phantomjs